services:
  mailserver:
    image: ghcr.io/docker-mailserver/docker-mailserver:edge
    container_name: mailserver
    hostname: mail
    domainname: vavilov.tj
    ports:
      - "25:25"
      - "143:143"
      - "993:993"
      - "587:587"
    volumes:
      - mail-data:/var/mail/
      - mail-state:/var/mail-state/
      - mail-config:/tmp/docker-mailserver/
      - /etc/localtime:/etc/localtime:ro
    environment:
      - ENABLE_SPAMASSASSIN=0
      - ENABLE_CLAMAV=0
      - ENABLE_FAIL2BAN=1
      - ONE_DIR=1
      - OVERRIDE_HOSTNAME=mail.vavilov.tj
      - ENABLE_POSTGREY=0
      - SPOOF_PROTECTION=0
      - ENABLE_SPAMASSASSIN_POSTFIX=0
    restart: always

  webmail:
    image: roundcube/roundcubemail:1.7.2-apache
    container_name: roundcube
    restart: always
    ports:
      - "8080:80"
    environment:
      - ROUNDCUBEMAIL_DEFAULT_HOST=mailserver
      - ROUNDCUBEMAIL_SMTP_SERVER=mailserver
    depends_on:
      - mailserver

volumes:
  mail-data:
  mail-state:
  mail-config:
